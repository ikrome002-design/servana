# Phase UI-09 — Merchant Administrator experience — proof

> **Status:** `local_complete`. Implementation and local acceptance are complete on
> `phase-ui-09-merchant-administrator-experience`. A pull request, exact-head CI, governance and
> merge are the remaining automated lifecycle; this record does not claim those future facts.

## Objective and authority

Deliver the Merchant Administrator's exact 23-page UI/UX-plan contract without changing business
authority: 15 truthful implemented pages, 8 visible but inert External-Gate-W entries, 0 planned and
0 removed. Governing sources are the role-specific UI/UX plan §6, §7, §9, §11–§13, §17–§21 and
§25; ADR-016…ADR-025; the Development Plan §9, §10.2, §10.3, §22, §24.1 and §80; the canonical
navigation map; and the permission matrix.

The phase began from verified `main`/UI-08 squash
`b435f4840649687bef3d54e61424d88047a10d4b`. UI-10, backend Phase 25, provider runtime and Gate-W
activation are explicitly outside scope.

## Recovered new-session state

The continuation recovered an intentionally dirty, uncommitted tree rather than restarting work.

| Check | Observed |
|---|---|
| branch | `phase-ui-09-merchant-administrator-experience` |
| `HEAD`, `origin/main`, merge-base | `b435f4840649687bef3d54e61424d88047a10d4b` |
| branch divergence / commits ahead | `0 0` / 0 |
| staged paths | 0 |
| Git operation in progress | none |
| working tree | expected UI-09 implementation, predecessor reconciliation, generated consumers and proof |
| surviving backend/Playwright/build worker | none |

One recovery snapshot was created outside the repository before continuation:

```text
C:\Users\nderu\AppData\Local\Temp\servana-ui09-recovery-20260812-154653
metadata       fa20e5aec760e3265c282956ba0ae14f1765ba8154c11a79434a16499d583960
binary patch   0cd54b6d8143508c38a222287bd8ed5070843d5fc01aa99e772e1c01427eb167
inventory      72d5bb0ace4df99e0328f9ffab907088130140493ed41bf5042742d7645623a1
tracked hashes 83
untracked copies 69
snapshot files 76
```

It was never applied, staged or committed.

## Docker Desktop recovery and backend gates

The prior parallel suite had run for 3,171.4 seconds when Docker Desktop's exec transport returned
HTTP 500 and stopped answering even read-only metadata calls. No Pest assertion failure was
reported. The authorized recovery restarted Docker Desktop itself, removed no volume, pruned
nothing and recreated no database. Docker Desktop 4.86 / Engine 29.7.2 recovered; all ten Servana
services returned and PostgreSQL was healthy. No prior test worker survived, so the interrupted run
was classified **environment/exec-transport failure**, not a product failure.

The already-complete serial suite was not repeated:

```text
php artisan test    exit 0    6,154.6 seconds
```

Its terminal stream exceeded the capture window and no existing compact aggregate could be
recovered; totals are therefore deliberately not invented.

The first recovered resource-safe parallel run was conclusive:

```text
2,966 passed / 14 skipped / 6 failed / 48,724 assertions
```

One failure was a transient invalid Meilisearch response seen only under the large run; its complete
independent suite passed 14/14. Five were stale consumers of canonical UI-09 activation:

| Observed problem | Root cause | Precise correction |
|---|---|---|
| pending-setup access expected a retired overview path | canonical setup/dashboard path changed | assert current overview path |
| UI-08 corrective matrix imposed a permanent route ceiling | a predecessor total was encoded as a maximum | preserve the UI-08 floor while allowing later authorized routes |
| Phase 23 named UI-07 as in flight | lifecycle consumer was not advanced | reconcile UI-07/UI-08 verified and UI-09 in flight |
| traceability route tokens were stale | documentation consumer drift | use the canonical UI-09 tokens |
| OpenAPI was one operation behind | generated projection predated the new safe owner read | regenerate from the route/schema authority |

Focused correction proof: 50 tests/1,241 assertions; Meilisearch 14/14; combined pending/search
22 tests/56 assertions. The corrected full run then completed with two processes:

```text
php artisan test --parallel
2,971 passed / 14 skipped / 0 failed / 48,712 assertions / 1 risky / exit 0
Pest 1,387 seconds; wall 1,569.6 seconds
```

The sole risky marker was the traceability test's then-empty UI-09 row loop, not an assertion
failure. This closeout adds the UI-09 rows and reruns that focused test with real assertions.

Final PHP checks after the last source correction:

```text
Pint                         1,880 files clean
Larastan level 8             1,437 analyzed / 0 errors
MerchantAdministratorExperienceApiTest   5 passed / 32 assertions
```

`UI09-STATIC-001` was the only final static finding: `StaffHistory` intentionally has nullable
`created_at` and no `updated_at`, but its PHPDoc omitted the existing Carbon attribute. Adding
`Carbon|null $created_at` fixed the type model without changing schema or runtime behavior.

## Delivered experience and authority boundaries

Final disposition from the canonical source:

```text
Merchant Administrator   23 = 15 implemented + 8 disabled_by_gate + 0 planned + 0 removed
authenticated global     160 pages
OpenAPI                   337 operations
permissions               169 total / 134 active / 35 planned
UI-09 permission delta    0 added / 0 activated
```

The fifteen live destinations are setup, dashboard, Get Started, Merchant Profile, branches,
branch detail, staff, subscription overview, plan, invoices, invoice detail, compensation summary,
payout approvals, finance-period reopen approvals and account/security. The eight inert entries are
payment attempts, billing recovery, five reports and notifications.

The host selects an experience, never authorization. Every backend read remains tenant-scoped;
foreign branch and invoice ULIDs do not enumerate. The owner dashboard now explicitly requires the
Merchant Administrator role. The staff endpoint is a narrow lifecycle projection under the
existing authority: no HR operational widening, phone/client PII or personnel contact export.
Mutations retain policies, validation and step-up. No plan amount, report metric, payment state,
Wallet collection, direct provider operation or R&E fact is fabricated in the browser.

## Deliberate creative and visual-quality pass

Every one of the 15 desktop-light captures was inspected, followed by representative dark, tablet
and mobile captures. Four surfaces were proven visually weak rather than rebuilt speculatively:

| Page | Before | Refinement |
|---|---|---|
| Dashboard | equal-weight neutral cards made owner state feel flat and generic | brand/status accents, warmer plan surface, clearer ownership labels and deliberate lower-card balance |
| Get Started | completed steps were struck through and visually dead | prominent warm progress panel, completion-coloured rows and the truthful label “Observed complete” |
| Branches | sparse directory cards left large unused workspace and weak hierarchy | owner portfolio summary, purposeful card density and clearer administrative/operational boundary copy |
| Subscription | one raw detail slab gave plan, state and dates equal weight | dominant plan/status composition, status pills, two-column period hierarchy and balanced supporting cards |

No new metric or client-calculated financial fact was introduced. The desktop capture viewport was
also corrected from Playwright's implicit 1280×720 default to the declared 1440×900 evidence size.

Targeted checks after refinement: ESLint on touched pages 0 errors; vue-tsc clean; three Vitest
files 12/12. The first targeted Get Started test correctly caught the changed status wording; the
component was restored to the approved truthful wording and the same test passed.

Final browser evidence:

```text
focused UI-09 Playwright    65 passed / 0 failed / 0 flaky / 0 skipped / exit 0 / 3.8 min
light axe                   all 15 pages, 0 serious / 0 critical
dark/drawer axe             representative pages and drawer, 0 serious / 0 critical
responsive                  360, 767, 768, 1024, 1025, 1280, 1440; 0 horizontal overflow
zoom                        200%; 0 horizontal overflow
```

The result is a clear owner-oriented hierarchy rather than a generic admin template, with the same
quality in light/dark and intentionally distinct mobile drawer, tablet rail and desktop sidebar.
Screenshots are implementation evidence, not UI-16-approved visual-regression baselines.

A signed-in local journey was also inspected through the real Laravel/Nginx/Vite stack: self-
registration, Magic Link, mandatory TOTP, recovery acknowledgement, five-step atomic setup and
dashboard, then staff, branch detail and account. The local catalogue required a clearly synthetic
active plan/price fixture because the development catalogue was empty. No production data, token or
secret was recorded; dark preference survived reload and the browser/proxy were finalized/removed.

## Frontend and whole-product browser gates

```text
ESLint                   exit 0 / 0 errors / 477 warnings (existing broad style/v-html warnings)
vue-tsc                  exit 0
Vitest final repeat      132 files / 1,364 tests / 0 failed / 218.74 seconds
Vite production build    1,264 modules / 11.43 seconds / exit 0
```

The first full Vitest run was 1,363/1,364: one historical UI-01 cash-up test reached its fixed
five-second budget with no assertion failure. It passed focused (1 passed/12 skipped, 4.61 seconds),
and the entire suite was repeated once and passed; no retry or assertion was added.

The first historical full Playwright attempt collected 1,237 and passed 1,131. Remaining failures
were stale path/fixture/gated-state consumers plus the real duplicate primary-navigation landmark.
They produced `UI09-A11Y-002`, `UI09-TEST-003` and `UI09-TEST-004`; each was fixed at its named root
without deleting, skipping or weakening a test.

The final authoritative run started its own production preview:

```text
Playwright collected/passed  1,255 / 1,255
failed/flaky/skipped          0 / 0 / 0
exit/duration                 0 / 2,775.7 seconds (46.2 minutes)
```

## Historical evidence preservation

The existing pre-run baseline was used; no replacement baseline was created. The whole-product run
rewrote exactly 41 frozen tracked predecessor artifacts and added exactly 127 UI-01 SHA-suffixed
screenshots. The 41 named paths were restored to the baseline bytes and the 127 exact additions
were moved—not broadly deleted—to the recoverable quarantine:

```text
C:\Users\nderu\AppData\Local\Temp\servana-ui09-gates\ui01-full-suite-additions
```

UI-07 current generated projections, the UI-08 defect index and every UI-09 artifact were
preserved. Final predecessor aggregates exactly match the baseline:

```text
UI-01  8 files   8b576dc7…f09a
UI-04 15 files   46a47780…a553
UI-05 10 files   c40443d0…0e04
UI-06 51 files   77cefa33…06a
UI-08 50 files   d7b55df4…a9fc
```

The final scope review then found one older test-written mutation outside the helper's directory
set: `docs/proof/ui-01/network/browser-evidence.json` contained refused localhost requests and the
pre-fix document-head errors. `UI09-EVIDENCE-001` restores that single file byte-for-byte from
verified base `b435f484`; it now has zero diff. No current generated projection was reverted.

No `git clean`, broad restore, reset or stash was used.

## Final production images and canonical-host proof

Only invalidated production targets were rebuilt, sequentially, from the stable runtime source:

```text
servana-ui09-php:audit
  sha256:90eeff392d1420c75606a4afd79d10143b76a9729e24dd81d825076b7d0162cb
  905,606,645 bytes / docker/php.Dockerfile prod / 135.6s / exit 0

servana-ui09-nginx:audit
  sha256:247251061fa4b80c0bd8783af53f393e3628c1fc21318bf4c3473d2ad0f3ac4d
  317,547,834 bytes / docker/nginx.Dockerfile prod / 14.0s / exit 0
```

The disposable `ui09-proof-net` topology used a production FPM container with an ephemeral valid
key and no project mount plus the Nginx image at loopback port 8109. `nginx -t` reported syntax OK
and configuration test successful. The final proof passed **44/44**: all 15 canonical pages, all 8
ordinary-shell gated paths, 8 compatibility paths, 3 wrong-branch-host checks, unknown-host refusal,
fingerprinted assets/MIME and absent JavaScript source maps. Containers and network were removed;
the ten-service development stack and its volumes were untouched.

The first disposable attempt's 31-byte proof-only key caused all account shells to fail before
route handling. Recreating only the disposable pair with a valid 32-byte ephemeral key resolved
44/44 with zero source change; this was a proof-fixture defect, not a product defect.

## Deterministic, dependency, secret and integrity gates

```text
account-host registry       8 accounts / 24 hosts current
navigation contract         160 pages current
UI-09 audit                 23 = 15 / 8 / 0 / 0; permissions 169 / 134 / 35
source inventory            160 pages / 8 roles / 40 documents / 7 brand assets / 61 images
OpenAPI                     286 paths / 337 operations; generated TypeScript current
focused contract parity     66 tests / 4,797 assertions
composer validate --strict  valid
composer audit --locked     no security advisories
npm audit --audit-level=high 0 vulnerabilities
gitleaks                    74 commits / about 39 MB / no leaks
git diff --check            clean
git fsck --full             no corruption or missing object; dangling objects informational
```

## Defect closure and residuals

The machine-readable ledger is `docs/frontend/audits/ui-09/defect-closure.json`. Thirteen closures are
`local_complete`: `UI09-DATA-001`, `UI09-STAFF-001`, `UI09-NAV-001`, `UI09-NAV-002`,
`UI09-A11Y-001`, `UI09-A11Y-002`, `UI09-TEST-001`…`UI09-TEST-004`, `UI09-VIS-001` and
`UI09-STATIC-001`, plus `UI09-EVIDENCE-001`. They cannot become `verified_complete` until the PR
merges with exact-head CI and truthful governance.

Residuals:

- Eight contract entries remain `disabled_by_gate`; owner/entry condition: the product owner opens
  External Gate W and the owning backend phases ship. UI-09 does not bypass this.
- Visual captures remain implementation evidence until UI-16 owns formal cross-account visual
  baselines.
- The serial suite's exit/duration are preserved but its compact totals were unrecoverable; final
  CI supplies a fresh exact-head backend aggregate.
- Required lifecycle after this commit: one PR, five-job exact-head CI, established truthful
  solo-maintainer governance, squash merge, tree equality and synchronized clean `main`.

No UI-10 work, backend Phase 25 work or Gate-W activation exists in this change.
