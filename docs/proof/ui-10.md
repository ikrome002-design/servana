# Phase UI-10 — Branch experience — proof

> **Status:** `verified_complete`. The historical local-completion narrative below is retained for
> auditability; final exact-head CI, governance, squash merge, tree integrity and cleanup are
> reconciled in the merge-closure section.

## Objective and authority

Implement the exact eighteen-page Branch Account experience at `branch.servana.ke`. Branch Manager
owns assigned-branch profile/calendar, service catalogue, branch day and cash-up submission, with
branch-scoped operational/financial reads. Finance, Front Office and HR mutations remain absent and
server-denied. UI-11, backend Phase 25 and provider runtime are excluded.

Authority: UI/UX plan §§2, 6–7, 9, 11–13, 15, 17–21, Phase UI-10 and Appendix A §7; Development Plan
§§3.1, 9, 14–25, 33–49, 66–70 and 80.2; canonical navigation map and permission matrix.

## Startup and predecessor reconciliation

- Clean synchronized `main`; HEAD = origin/main = UI-09 squash `84b7f803…`; divergence 0/0.
- PR #59 final reviewed head `59d10333340d66b2fc900783964ccbc4581ec9e4`; squash
  `84b7f803…`; merged `2026-08-12T17:06:03Z`; equal tree `3077b246…`.
- Exact-head run `31618625302`: Backend, Frontend, Docker, Security and E2E successful.
- Governance comment `5269937398`; zero reviews and blank decision — not independent approval.
- UI-09 proof/checklist/closures/PROGRESS/CHANGELOG/traceability promoted to `verified_complete`.

## Proven problem and root cause

Baseline canonical status was 6 implemented, 9 planned and 3 gated. Several completed backend
capabilities existed only on legacy prefixed routes or account-authority surfaces. Dashboard/Branch
Day truth could not be obtained by summing page-one API results, while reusing Finance, Front Office,
HR or raw Audit views would expose the wrong controls/data or require permissions Branch must not
hold. The root cause was an incomplete host-relative role presentation plus four missing narrow
assigned-branch reads—not missing Branch business authority.

## Readiness and final disposition

`page-readiness-matrix.json` has exactly 18 rows: A4 / B1 / C5 / D5 / E3 / F0. The final generated
contract is **15 implemented + 3 disabled_by_gate + 0 planned + 0 removed = 18**; global authenticated
pages remain 160.

Implemented routes:

```text
/dashboard                         /get-started
/branch/profile                    /branch/calendar
/branch/day                        /services
/staff                             /operations/queue
/operations/appointments           /finance/invoices
/finance/payments                  /finance/receipts
/cash-up                           /audit
/account
```

Gated with no route/component/network runtime: `/reports` (21N), `/subscription/payment` (20D-W
after Gate W), `/notifications` (21N after 20D-W). They remain visible and inert; no fake Wallet
state, success, notification, report metric, PDF or provider call exists.

## Correct implementation

- Shared left-navigation family: exact eight groups, persistent desktop sidebar, 768–1024 rail,
  accessible 360–767 drawer, active branch identity and account controls.
- `BranchWorkspaceReadModel` returns authoritative service, staff, queue, appointment, invoice,
  validation, receipt, cash-up, billing and closure facts for the assigned branch. Currency values
  remain integer minor units and are grouped by currency.
- Four new GET operations, each behind Sanctum, active merchant/account/permission middleware,
  scoped route binding and a Form Request:
  `branches/{branch}/dashboard`, financial-visibility invoices/payment-records, and audit-events.
- Financial and audit resources omit client contact, payment references, maker identity and raw
  Audit workflow. Collections paginate.
- Branch Profile/Calendar/Day use existing policies and state machines. Service catalogue retains
  Branch mutation authority. Staff/queue/appointments/financial views expose no forbidden takeover,
  HR, Front Office or Finance controls. Cash-Up remains maker-only; Finance is checker.
- Own Account reuses own-scope Magic Link/MFA/session/theme contracts with Branch-specific copy.

OpenAPI is generated at **290 paths / 341 operations**, exactly +4 from 337. Generated TypeScript and
`api:contract:check` are current. Permission catalogue remains **169 total / 134 active / 35 planned**;
UI-10 adds 0 and activates 0.

## Focused verification

```text
php artisan test tests/Feature/Ui10/BranchExperienceApiTest.php
PASS — 4 tests / 57 assertions / PostgreSQL

npm run typecheck
PASS

npx vitest run <four focused files> --maxWorkers=1
PASS — 4 files / 32 tests

npx playwright test tests/e2e/ui-10-branch-experience.spec.ts --project=chromium --reporter=line
PASS — 52 / 52 / 145.6 seconds
```

Backend proof covers truthful assigned-branch facts, pagination, masking, unassigned/foreign-tenant/
wrong-account 403/404 and forbidden invoice/payment/HR/checker mutations. Browser proof covers all 15
live routes, 3 inert gates, exact navigation, missing/wrong account denial, forbidden-control absence,
360/767/768/1024/1025/1280/1440, 200% equivalent, light-default/dark persistence and 15 light plus
dark/drawer axe scans with 0 serious/critical findings.

Failed-run RCA is preserved in `browser-proof.json` and `defect-closure.json`. Focused fixture and
gesture defects were corrected without weakening protected behavior. The first whole-product run
passed 1,314 cases and exposed eight stale route/inventory consumers; focused correction passed 8/8.
The replacement passed 1,324 cases but its cold first lazy route missed the five-second default
heading bound; the captured tree held only the empty toast landmark while every later appointment
case passed. A bounded 15-second semantic readiness assertion passed on a fresh preview, followed by
the authoritative 1,325/1,325 run. The footer case exposed a real shared defect: at 640×450, rendered
footer height 105px exceeded the 88px reserve. The existing zoom fallback now applies through 32rem
and the test asserts computed reserve geometry.

## Creative review

All 15 pages were reviewed at 1440×900 light, with dark Dashboard, tablet, mobile and high-density
Dashboard/Cash-Up/Account. Dashboard and Branch Day carry the required operational hierarchy;
read-only context is unmistakable; empty states name responsible roles; Cash-Up separates immutable
expected from editable counted values. Targeted refinements humanize blocker keys, label session
account experiences and capture exact viewport evidence. No metric or raw color was invented. Formal
cross-account visual approval remains UI-16.

## Defect protocol and residual ownership

Eleven closures are `local_complete`: UI10-DATA-001, UI10-NAV-001, UI10-AUTH-001,
UI10-RESP-001, UI10-VIS-001, UI10-TEST-001…UI10-TEST-005 and UI10-CI-001. Full observed/evidence/root-cause/
fix/test/future-merge condition is in `defect-closure.json`; none may become `verified_complete`
before merge reconciliation.

Deferred owners:

- 20D-W after Gate W: Wallet collection/recovery.
- 21N after 20D-W: Branch reports, scheduled/private delivery and notifications.
- UI-11: Human Resource 19-page experience, only after UI-10 lifecycle closure.
- UI-16: formal cross-account visual regression approval.
- Phase 25: deployment, under separate authorization.
- Scheduling/service-session test owner before Phase 25: known UI07-ENV-001 Nairobi timing residual.

## Final gate ledger

- Deterministic sources: navigation 160/8; Branch 18 = 15 implemented + 3 gated; UI-09 and UI-10
  artifact checks green; OpenAPI 290 paths / 341 operations; permissions 169/134/35 with zero delta.
- Pint: 1,892 files clean. Larastan level 8: 1,448 analyzed / 0 errors.
- Final PostgreSQL backend: 2 processes, **2,976 passed / 14 skipped / 0 failed / 48,859
  assertions / 1,714.67 seconds**. The first full run's 2,973 pass / 3 failure history is retained as
  UI10-TEST-001; focused correction passed 60 tests / 4,614 assertions.
- Frontend: ESLint 0 errors / existing 477 warnings; `vue-tsc --noEmit` clean; Vitest **135 files /
  1,371 tests**; Vite 8.1.5 built **1,277 modules**.
- Dedicated Playwright: 52/52 in 145.6 seconds. Final whole product: **1,325 collected / 1,325
  passed / 0 failed / 0 flaky / 0 skipped / exit 0 / 46.8 minutes**. Failed histories of
  1,314/1,322 and 1,324/1,325 are retained with RCA.
- Accessibility: 15 light page scans plus representative dark Dashboard/open drawer; zero serious
  and zero critical findings. Responsive: 360/767/768/1024/1025/1280/1440 plus 200% equivalent,
  with no normal horizontal overflow and valid fixed-footer reserve.
- Historical evidence: 54 individually proven test-written predecessor files restored to the UI-09
  merge bytes; 124 exact accidental UI-01 screenshots removed; frozen predecessor paths have zero
  Git drift. Current UI-07 projections were not restored and pass `nav:check`. Initial CI then
  proved that UI-06 `public-route-matrix.json` is also a current router projection inside an
  otherwise historical directory; UI10-CI-001 regenerates only that changed file and the baseline
  now excludes it from frozen restoration. UI source, UI-09 and UI-10 checks are current.
- Final targeted current-consumer check found two stale historical traceability route claims;
  UI10-TEST-005 moves only their frontend evidence to `branch.staff` and
  `branch.branch-calendar`. Phase23TraceabilityTest then passed 15/15; the same targeted run had
  already passed UI07NavigationContractTest 39/39 and the UI-10 API test 4/4.
- Production: PHP `sha256:56c853a1b2c8f4b07fa0845b04df50da5288cead59799673fc0fa17d2249cd43`
  (908,438,922 bytes) and Nginx
  `sha256:fd348f4d45cc9ed8c761d854280621f9431099caf6e41fbcad3b10ecb2f5000b`
  (317,579,259 bytes). `nginx -t` passed; disposable no-volume topology passed **38/38** canonical
  host/route/gate/collision/asset/source-map checks and was removed.
- Security/integrity: Composer 0 advisories; npm 0 vulnerabilities; gitleaks 75 commits / 39.46 MB /
  no leaks; `git diff --check` clean; `git fsck --full` clean apart from informational dangling
  unreachable objects.

Local-completion implementation commit `c73d1be63a58b67aad343f9806855f607d29d37d` opened non-draft
PR #60 with the exact required title. Initial exact-head CI exposed UI10-CI-001 below. The exact
next action is one narrow generated-projection correction commit, replacement exact-head five-job
CI, truthful governance, squash merge, branch deletion and clean synchronized `main`. UI-11 and
Phase 25 are not started; Gate W remains closed.

## Initial pull-request CI correction

PR #60 initial exact-head run `31665290449` ran against implementation head
`c73d1be63a58b67aad343f9806855f607d29d37d`:

- Docker, Security and E2E succeeded; E2E passed the full Linux Playwright job.
- Frontend stopped at `generate-ui06-artifacts.mjs --check` before later steps because exactly
  `docs/frontend/audits/ui-06/public-route-matrix.json` was stale.
- Backend passed Pint, Larastan, traceability and performance contracts, then passed **3,541** Pest
  tests / **51,545** assertions with 5 skipped; its only failure was
  `Ui06GeneratedArtifactStalenessTest`, naming the same file.

Root cause: the UI-10 restoration treated the entire UI-06 audit directory as frozen, contrary to
the already documented UI09-CI-001 rule that this route matrix is generated from current route
modules. The correction regenerates UI-06 artifacts and retains only the changed public route
matrix, then records that exact mixed-directory exception. No runtime source, route, authority,
permission, OpenAPI operation or screenshot changed. Replacement exact-head five-job CI is required
before governance or merge.

## Merge closure — reconciled live during UI-11

- Non-draft PR [#60](https://github.com/ikrome002-design/servana/pull/60), title
  `Phase UI-10: Implement branch experience`, merged at `2026-08-13T05:04:49Z`.
- Final reviewed source head: `e5f2a6fb3201e8369333569618ed11a3d8c56ac0`; normal squash merge:
  `d7988dca90ad8694dee4c0b066f2581155fcb40a`.
- Final exact-head run `31667335687`: Backend, Frontend, Docker, Security and E2E all concluded
  `success`. Initial failed run `31665290449` remains preserved in the RCA above.
- Governance comment `5276187980`; submitted reviews `0`; approvals `0`; review decision blank.
  This is truthful solo-maintainer governance evidence and is **not** independent approval.
- Source-head tree and squash-merge tree both equal
  `0a75deddaba345c9d501605fd96c0c4108d41708`; the reviewed tree is intact.
- Local and remote `phase-ui-10-branch-experience` branches are absent. UI-11 startup proved clean,
  synchronized `main` at the squash SHA before creating its phase branch.
- All eleven UI-10 closures are promoted from `local_complete` to `verified_complete`.
